function matches (hipotesis, instance) {
  return false
}

function versionSpace (attributeSet, attributeValuesSet, classes, trainingInstances) {
  const maximallyGeneric = new Set()
  const minimallySpecific = new Set()

  return (maximallyGeneric, minimallySpecific)
}

module.exports = { versionSpace, matches }
